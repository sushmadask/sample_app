(function () {
    app.controller('ListingController', ['$scope', '$log', '$http', '$state', function ($scope, $log, $http, $state) {
        window.scrollTo(0, 0);
        $scope.init();
        $scope.init = function () {
            
        };

        $scope.open = function(){

        };
    }]);
})();